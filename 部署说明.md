# Socialization 部署说明

本文面向部署与维护者，覆盖环境准备、安装、配置、启动、迁移、备份恢复与故障排查。使用说明见 [使用说明.md](./使用说明.md)。

## 一、部署形态

- 单机本地部署（原生 Windows，无 Docker / WSL / PostgreSQL）。
- 前端：Vite 开发服务器（开发）/ 静态构建产物（`frontend/dist`，可由 FastAPI 托管）。
- 后端：FastAPI + Uvicorn（`127.0.0.1:8000`）。
- 数据库：SQLite 单文件 `data/socialization.db`（WAL 模式）。
- 数据目录：`data/`（数据库、密钥、上传文件、备份、日志）。

## 二、环境要求（以 installed-apps.txt 为准）

| 软件 | 版本 | 用途 |
|---|---|---|
| Windows | 10/11 | 运行系统 |
| Python | 3.13.5（64 位） | 后端运行时（内置 SQLite 3.49+） |
| Node.js / npm | 24.13.0 / 11.6+ | 前端构建与开发 |
| Git | 2.55+ | 版本管理（可选） |
| PowerShell | 5.1 或 7.x | 脚本（脚本兼容两者） |
| 浏览器 | Chrome / Edge 最新 | 访问界面 |

无需安装 Docker、WSL、PostgreSQL、Redis。

## 三、安装步骤

```powershell
cd E:\Socialization

# 1. 复制环境模板（可选，默认值即可运行）
Copy-Item .env.example .env

# 2. 一次性初始化：创建 Python 虚拟环境、安装后端/前端依赖、初始化数据目录、执行数据库迁移
.\scripts\setup.ps1
```

`setup.ps1` 会依次执行：`data/` 目录创建 → `backend/.venv` 虚拟环境 → `pip install -r backend/requirements.txt` → `alembic upgrade head` → `npm install`。

## 四、配置（.env）

| 变量 | 默认值 | 说明 |
|---|---|---|
| `APP_NAME` | Socialization | 应用名 |
| `APP_VERSION` | 0.1.0 | 版本号 |
| `DATABASE_URL` | `sqlite:///data/socialization.db` | SQLite 路径（相对项目根，自动转绝对路径） |
| `DATA_DIR` | `data` | 数据目录（上传/备份/密钥） |
| `BACKEND_HOST` / `BACKEND_PORT` | 127.0.0.1 / 8000 | 后端监听（保持本机） |
| `FRONTEND_PORT` | 3000 | 前端端口 |
| `CORS_ORIGINS` | `["http://127.0.0.1:3000","http://localhost:3000"]` | JSON 数组；浏览器扩展收藏时需加入扩展来源 |
| `LOG_LEVEL` | INFO | 日志级别 |
| `HTTPS_PROXY` | 空 | 访问 AI 接口受限时配置代理 |

修改 `.env` 后需重启后端生效。

## 五、启动与停止

```powershell
.\scripts\dev.ps1              # 一键启动前后端；按 Enter 停止
# 或分开启动：
.\scripts\dev-backend.ps1      # 后端 http://127.0.0.1:8000
.\scripts\dev-frontend.ps1     # 前端 http://127.0.0.1:3000
```

生产方式（可选）：`npm run build` 生成 `frontend/dist`，由 FastAPI 托管静态文件后仅运行 uvicorn 单进程即可。

## 六、数据库与迁移

- 迁移工具：Alembic，脚本位于 `backend/migrations/versions/`。
- 当前基线：`0001_empty_initial → 0002_p0_tables → 0003_seed_data → 0004_p1_tables → 0005_p2_tables (head)`。

```powershell
cd backend
.\.venv\Scripts\python.exe -m alembic upgrade head    # 升级到最新
.\.venv\Scripts\python.exe -m alembic current          # 查看当前版本
.\.venv\Scripts\python.exe -m alembic downgrade -1     # 回退一步
```

数据约定：UUID 主键、UTC 时间、JSON 字段、软删除（`deleted_at`）、FTS5 全文索引（文档切块与笔记）。

## 七、备份与恢复

### 界面操作

- 设置 → 数据备份：导出 JSON / 导入 JSON / 创建备份 / 恢复（恢复前自动生成安全快照）/ 下载备份。

### 命令行

```powershell
.\scripts\backup.ps1    # 生成 data\backups\socialization-时间戳.db
```

### 手动备份/恢复要点

- 备份文件为 SQLite 一致性快照，可直接复制。
- 恢复 = 停止后端 → 用备份文件替换 `data/socialization.db` → 删除同目录 `-wal`/`-shm` 文件 → 启动后端；应用内“恢复”按钮会自动完成上述流程。
- 恢复旧快照后，数据库结构若落后于当前迁移，请先执行 `alembic upgrade head`。
- API Key 密文依赖 `data/.secret.key`；迁移到新机器时需同时携带该文件，否则已加密的 Key 无法解密（可删除并重新配置）。

## 八、测试与质量检查

```powershell
.\scripts\test.ps1
# 后端 ruff + pytest（72 项）；前端 eslint + tsc + vitest（11 项）
```

单跑命令：

```powershell
cd backend; .\.venv\Scripts\python.exe -m ruff check --no-cache .; .\.venv\Scripts\python.exe -m pytest
cd frontend; npm run lint; npm run typecheck; npm run test; npm run build
```

## 九、安全说明

- 服务仅监听 `127.0.0.1`，默认不暴露局域网。
- API Key 加密存储（AES-256-GCM），日志与异常自动过滤敏感字段；前端仅返回掩码与末四位。
- 密钥文件 `data/.secret.key` 属敏感文件：**勿提交版本库、勿删除**（删除=重置全部密钥）。
- 笔记以结构化 JSON 保存（无原生 HTML 注入面）；网页收藏仅抓取纯文本。
- 删除、恢复、导入均有二次确认；人物彻底删除会级联清理关联数据。
- 浏览器扩展收藏当前页时会读取页面正文并发送到本地服务，仅在可信页面使用。

## 十、目录结构

```text
Socialization/
├── backend/        # FastAPI（app/models|schemas|repositories|services|providers|api、migrations、tests）
├── frontend/       # React（src/pages|components|api|lib、tests、extension）
├── scripts/        # setup / dev / test / backup
├── data/           # socialization.db、.secret.key、uploads、backups、exports、logs（gitignored）
├── docs/           # 技术文档
├── PRD.md / DEVELOPMENT_PLAN.md / TASKS.md / installed-apps.txt   # 只读规划文档
├── 使用说明.md / 部署说明.md
```

## 十一、常见问题排查

| 现象 | 排查 |
|---|---|
| `setup.ps1` 安装依赖慢/失败 | 配置镜像：npm registry、pip 清华源；确认网络与代理 |
| 端口 8000/3000 被占用 | `Get-NetTCPConnection -LocalPort 8000,3000 -State Listen`，关闭占用进程后重启 |
| 前端请求 502 | 确认 vite 代理指向 `127.0.0.1:8000` 且后端已启动 |
| `alembic upgrade head` 报错 | 确认 `data/` 存在、`.env` 数据库路径正确；必要时先备份再迁移 |
| 中文乱码 | 代码/文档为 UTF-8；PowerShell 控制台 `chcp 65001`；installed-apps.txt 为 GBK |
| 恢复后页面数据未变 | 恢复后刷新浏览器；确认恢复的是目标快照 |
| AI 功能不可用 | 设置页测试连接 → 选择模型 → 对话页选择提供商/模型 |

## 十二、升级与维护

- 升级前先创建备份；升级后执行 `alembic upgrade head`。
- 前端依赖以 `package-lock.json` 锁定；后端以 `requirements.txt` 约束。
- 关键变更记录在 Git 提交信息与 TASKS.md/DEVELOPMENT_PLAN.md（只读文档，变更需先确认）。
