# Socialization 使用说明

本文面向软件使用者（即软件所有者本人），介绍各功能模块的日常用法。部署与安装见 [部署说明.md](./部署说明.md)。

## 快速开始

```powershell
cd E:\Socialization
.\scripts\setup.ps1    # 首次安装（依赖 + 数据库迁移）
.\scripts\dev.ps1      # 启动前后端，按 Enter 停止
```

浏览器打开 `http://127.0.0.1:3000`。首次使用建议按顺序：**设置 AI 提供商 → 新建人物 → 记录互动 → 建立话题笔记 → 使用 AI 助手**。

## 首页

- 系统状态：后端与 SQLite 连接状态（健康检查）。
- 统计卡片：人物 / 互动 / 话题 / 文件数量。
- 提醒：待跟进事项、今日复习任务（点击直接跳转话题）。
- 快捷入口：新建人物、人物列表、记录互动、互动记录、生成周报。

## 人物管理

### 新建与编辑人物

- 点击“新建人物”填写：姓名（必填）、昵称、关系类型、熟悉程度（1–6）、认识时间/地点/途径、公司/职业/所在地、摘要、隐私等级。
- 编辑入口在人物列表“编辑”按钮或详情页右上角。

### 人物详情页

- **标签**：勾选或取消标签（标签在设置页/列表页创建）。
- **人物事实**：类型（喜好/兴趣/禁忌/性格印象等）、内容、来源（用户记录/对方亲口表达/AI 推测）、确认状态徽标、敏感标记。AI 来源的事实显示“未确认”，不会自动当作事实。
- **重要日期**：生日/纪念日等。
- **待跟进**：手动添加，或在互动里填写“后续事项”后自动生成；可勾选完成。
- **自定义字段**：在设置页创建字段定义，此处填写取值（如“喜欢的咖啡”）。
- **人物关系**：选择另一位人物并指定关系类型（同事/同学/朋友/家人/上下级/伴侣/共同兴趣/由某人介绍）；详情页会显示环形关系图，关系双向可见。
- **聊天简报**：一键生成 AI 简报（开场方式、可延续话题、避免重复询问的内容、边界提醒），需要已配置可用 AI 提供商。
- **时间线**：互动 + 事实 + 重要日期按时间倒序汇总。

## 互动记录

- 记录互动：标题、时间、方式（面对面/电话/微信/聚会/工作/运动/吃饭/其他）、地点、时长、摘要、对方新增信息、情绪状态、自己表现、正面反馈、冷场或问题、后续事项、隐私等级；关联一位或多位人物。
- **后续事项**会自动生成到人物详情的“待跟进”。
- 列表支持按人物筛选、删除（需确认）。
- **提取**：AI 从互动中提取事实与待跟进建议，出现在“待确认”区域；点击“全部确认写入”后才会写入人物事实（标记为 AI 推测/未确认）与待跟进。
- **待确认**：查看该互动尚未确认的提取结果。
- **复盘**：一键生成 AI 复盘（倾听/追问/表达/共情等角度）。

## 话题知识库

- 左侧分类树（可新建/删除分类），右侧话题列表（搜索、按分类筛选）。
- 新建话题：名称、分类、掌握程度（1–6）、简介。
- 话题详情：关联人物、关联文件（选择已上传文件）、**笔记编辑器**（标题/加粗/斜体/高亮/引用/表格/图片/代码/待办/列表；编辑后约 1.5 秒自动保存，可点“立即保存”；多人同时编辑冲突时提示刷新）。
- 学习一个话题后，系统自动创建复习任务；到“复习计划”页打卡，间隔按反馈顺延（忘记 1 天 / 模糊 3 天 / 掌握 7 天 / 非常熟练 30 天）。

## 文件资料库

- 支持格式：PDF、DOCX、PPTX、XLSX、TXT、Markdown、HTML、CSV，以及图片（OCR）、音频/视频（转写）。
- 上传后自动解析并切块（每段约 700 字、含重叠），状态显示“已完成/解析失败”等；失败可点“处理”重试。
- 可关联人物/话题/对话；关联后，AI 对话会自动检索这些文件的内容。
- **网页收藏**：粘贴网页 URL → “收藏网页”，自动抓取正文并入库。
- 点击文件可查看切块内容；删除需确认。

## 全局搜索

导航“搜索”输入关键词，结果按人物 / 话题 / 互动 / 笔记 / 文件片段分组；点击可跳转详情。

## AI 助手

### 前置：设置 AI 提供商

1. 进入“设置”→ 新建提供商：选择类型（DeepSeek / OpenAI / Ollama（本地）/ OpenAI 兼容接口）、填写 Base URL 与 API Key、超时与重试。
2. 保存后显示“API Key 已配置（••••末四位）”，可“测试连接”。
3. “同步模型”或手动添加模型（模型 ID），并可在模型列表启停。

### 使用对话

- 新建对话 → 选择提供商/模型（切换提供商后模型列表自动刷新）。
- 可关联人物、话题、文件（检索范围）。
- 发送消息：流式输出；AI 消息带“AI 生成”标记，可**复制**、**重新生成**；生成中可**停止**。
- 关联人物时，AI 上下文只包含“已确认/用户观察”且非敏感的事实；关联话题时包含话题摘要与笔记；关联文件时包含检索到的相关片段，回答下方会显示“引用：文件名”。
- 对话历史自动保存，刷新后保留。

## 社交练习

- 选择 13 种练习场景之一（陌生人/同事/饭局/聚会/内向者/上级/冷场/分歧/拒绝/边界/感谢等）开始练习。
- 与 AI 角色对话（流式）；结束后点“结束并评分”，查看 10 个维度评分与总结建议。

## 复习计划

- 今日到期复习任务列表；选择“忘记 / 模糊 / 掌握 / 非常熟练”打卡，下次复习时间按间隔顺延。

## 设置

- **AI 提供商与模型**：见上文。
- **自定义字段定义**：创建字段（单行文本/多行文本/数字/日期/链接/布尔等），人物详情页填写。
- **数据备份**：
  - 导出 JSON（下载全量数据，不含 API Key 明文）。
  - 导入 JSON（会先清空业务数据再写入，请谨慎；导入后自动刷新）。
  - 创建备份（SQLite 一致性快照，可下载）。
  - 恢复（需二次确认，恢复前自动生成安全快照）。
- **人物 CSV 批量导入**：调用接口 `POST /api/import/persons-csv`（列：name/姓名、nickname/昵称、organization/公司、occupation/职业、location/所在地、relationship_type/关系）。

## 长期记忆、目标与周报

- “设置”页下方暂以接口方式提供：`GET/PATCH /api/profile` 维护表达偏好与社交目标；`GET/POST /api/memory` 管理长期记忆（接受后注入对话上下文）。
- 首页“生成周报”根据最近一周互动生成成长报告。

## 数据安全提示

- 所有数据默认只在本机 `127.0.0.1`，不对外网开放。
- API Key 加密保存于数据库（密钥文件 `data/.secret.key`），前端只显示掩码，日志自动过滤。
- **删除 `data/.secret.key` 等于重置全部已加密 API Key**（需重新配置，旧密钥无法恢复）。
- 建议定期“创建备份”，并把 `data/backups` 复制到其他磁盘/网盘。

## 常见问题

| 问题 | 处理方式 |
|---|---|
| AI 对话报“请先配置并启用 AI 提供商” | 到设置页配置提供商、测试连接、选择模型，并在对话页选择提供商与模型 |
| 简报/提取/复盘/练习报 AI 错误 | 确认提供商可用（测试连接通过）且已选模型 |
| 上传文件状态“解析失败” | 文件页点“处理”重试；图片 OCR/音视频转写需要提供商支持对应能力 |
| 笔记自动保存提示冲突 | 内容可能被其他编辑修改，刷新后重试 |
| 忘记 API Key | 到设置页清除后重新输入（密钥文件未删除则无需重配全部） |
| 端口被占用 | 关闭占用 3000/8000 的程序后重新 `dev.ps1` |
