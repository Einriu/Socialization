# Socialization 使用说明

本文面向软件使用者（包括只拿到 EXE 的其他人），介绍如何安装、启动和使用全部功能。部署与源码安装见 [部署说明.md](./部署说明.md)。

## 1. 软件简介

Socialization 是一款个人社交关系管理 + AI 辅助工具，核心能力：

- **人物库**：记录人物资料、事实、重要日期、关系、待跟进事项。
- **互动记录**：记录每次交流，AI 自动提取事实与后续事项，并可复盘。
- **话题知识库**：分类整理话题，内置富文本笔记编辑器。
- **文件资料库**：上传 PDF/Word/PPT/Excel/图片/音视频等，AI 可检索。
- **AI 助手**：基于人物、话题、文件上下文的流式对话。
- **社交练习**：自定义场景，与多个 AI 角色进行多人对话练习并评分。
- **复习计划**：按遗忘曲线安排话题复习。

所有数据默认只保存在本机，服务只监听 `127.0.0.1`，不对外网开放。

## 2. 安装与启动

### 方式一：EXE 桌面版（推荐）

1. 双击 `Socialization.exe` 即可使用，无需安装 Python、Node.js 等任何环境。
2. 首次启动较慢（10–30 秒），因为程序要先解包并自动创建数据库，属正常现象。
3. 程序打开自己的窗口（1280×860，可调整），无需浏览器。
4. 系统要求：Windows 10/11，需有 Edge WebView2 运行时（Win10/11 一般自带；缺失时到微软官网安装“WebView2 Runtime”即可）。
5. 杀毒软件可能将 PyInstaller 打包的程序误报，请在确认来源可信后选择“允许”。
6. 所有数据保存在 **EXE 同目录的 `data` 文件夹**中（数据库、上传文件、备份等）。移动 EXE 时请连 `data` 文件夹一起移动，否则数据不会跟着走。

### 方式二：开发模式（有源码时）

```powershell
cd E:\Socialization
.\scripts\setup.ps1    # 首次安装：Python 虚拟环境、依赖、数据库迁移
.\scripts\dev.ps1      # 启动前后端，按 Enter 停止
```

启动后浏览器打开 `http://127.0.0.1:3000`。开发模式的数据保存在项目根目录 `E:\Socialization\data`。

> **注意**：EXE 桌面版与开发模式各自使用独立的数据库，数据**不互通**。迁移方法见第 16 节。

## 3. 首次使用建议顺序

1. 进入 **设置** → 新建并测试一个 **AI 提供商**（所有 AI 功能的前提）。
2. 在 **首页** 点击“新建人物”，录入身边的重要人物。
3. 记录一次 **互动**，体验 AI 提取与复盘。
4. 建立 **话题** 并写笔记，到 **复习** 打卡。
5. 体验 **AI 助手** 与 **社交练习**。

## 4. 界面总览

顶部导航：**首页 / 人物 / 互动记录 / 话题 / 文件 / 搜索 / 练习 / 复习 / AI 助手 / 设置**。右上角按钮可在浅色与深色主题间切换。

## 5. 首页

- **系统状态**：后端与 SQLite 连接状态（健康检查），异常时页面会明确提示。
- **统计卡片**：人物 / 互动 / 话题 / 文件数量。
- **提醒**：待跟进事项、今日到期的复习任务（点击可跳转）。
- **快捷入口**：新建人物、人物列表、记录互动、互动记录、生成周报。
- **生成周报**：根据最近一周的互动，由 AI 生成一段成长总结。

## 6. 人物管理

### 新建与编辑

- 新建人物需填写姓名；可选昵称、关系类型、熟悉程度（1–6）、认识时间/地点/途径、公司/职业/所在地、摘要、隐私等级。
- 编辑入口：人物列表的“编辑”按钮，或详情页右上角。

### 人物详情页

- **标签**：勾选或取消标签（标签在设置页/人物列表创建）。
- **人物事实**：类型（喜好/兴趣/禁忌/性格印象等）、内容、来源（用户记录/对方亲口表达/AI 推测）、确认状态、敏感标记。AI 推测的事实默认“未确认”，需人工确认后才参与 AI 上下文。
- **重要日期**：生日、纪念日等。
- **待跟进**：手动添加，或在互动中填写“后续事项”后自动生成；可勾选完成。
- **自定义字段**：字段定义在设置页创建，此处填写取值（如“喜欢的咖啡”）。
- **人物关系**：选择另一位人物并指定关系类型；详情页显示环形关系图，关系双向可见。
- **聊天简报**：一键生成 AI 简报（开场方式、可延续话题、避免重复询问的内容、边界提醒），需要已配置可用的 AI 提供商。
- **时间线**：互动、事实、重要日期按时间倒序汇总。

## 7. 互动记录

- **记录互动**：标题、时间、方式（面对面/电话/微信/聚会/工作/运动/吃饭/其他）、地点、时长、摘要、对方新增信息、情绪状态、自己表现、正面反馈、冷场或问题、后续事项、隐私等级；可关联一位或多位人物。
- 互动中的“后续事项”会自动进入人物详情的“待跟进”。
- 列表支持按人物筛选；删除需确认。
- **AI 提取**：AI 从互动中提取事实与待跟进建议，先出现在“待确认”区域；点击“全部确认写入”后才写入人物事实与待跟进。
- **复盘**：一键生成 AI 复盘（倾听、追问、表达、共情等角度）。

## 8. 话题知识库

- 左侧为分类树（可新建/删除分类），右侧为话题列表（支持搜索、按分类筛选）。
- 新建话题：名称、分类、掌握程度（1–6）、简介。
- 话题详情：
  - **关联人物 / 关联文件**：选择已上传的文件后，AI 对话可检索其内容。
  - **笔记编辑器**：支持标题（H1/H2/H3）、加粗/斜体/高亮、引用、表格、图片（本地上传）、代码块、待办清单、普通列表；支持 Markdown 快捷输入；可撤销/重做。
  - 笔记编辑后约 1.5 秒自动保存，也可按 `Ctrl+S` 立即保存；页面显示保存状态。
- 学习话题后系统自动创建复习任务；到“复习计划”打卡。

## 9. 文件资料库

- 支持格式：PDF、DOCX、PPTX、XLSX、TXT、Markdown、HTML、CSV，图片（OCR 文字识别）、音频/视频（语音转写）。
- 上传后自动解析并切块（每段约 700 字、含重叠），状态显示“已完成 / 解析失败”；失败可点“处理”重试。
- 可关联人物 / 话题 / 对话；关联后 AI 对话会自动检索这些文件的内容，并在回答下方显示“引用：文件名”。
- **网页收藏**：在文件页粘贴网页 URL → “收藏网页”，自动抓取正文入库。
- 点击文件可查看切块内容；删除需确认。

## 10. 全局搜索

导航进入“搜索”，输入关键词后结果按人物 / 话题 / 互动 / 笔记 / 文件片段分组展示，点击可跳转到详情。

## 11. AI 助手

### 前置：配置 AI 提供商

1. 进入 **设置** → 新建提供商：选择类型（DeepSeek / OpenAI / Ollama 本地 / OpenAI 兼容接口），填写 Base URL 与 API Key、超时与重试参数。
2. 保存后 API Key 显示为掩码（“••••末四位”），可点击“测试连接”验证。
3. “同步模型”拉取可用模型，也可手动添加模型 ID；模型列表可启停。

### 使用对话

- 新建对话 → 选择提供商/模型（切换提供商后模型列表自动刷新）。
- 可关联人物、话题、文件，限定 AI 的检索范围。
- 发送后流式输出；AI 消息可**复制**、**重新生成**；生成中可**停止**。
- 关联人物时，AI 上下文只包含“已确认/用户观察”且非敏感的事实；关联话题时包含话题摘要与笔记；关联文件时包含检索到的相关片段。
- 对话历史自动保存，刷新页面后保留。

## 12. 社交练习（核心功能）

社交练习用于模拟真实社交场景，与多个 AI 角色进行多人对话。完整流程分两步：**先创建场景并生成背景，再进入练习会话**。

### 第一步：创建场景（导航“练习”）

页面按三步设置场景：

1. **选择交流渠道**：线下社交 或 线上（微信等）。
2. **定义场景**，两种方式任选：
   - **用标签组合**：从内置标签库中勾选“场合 / 谈话背景 / 对象类型”标签自由组合；还可以输入**自定义标签**（如“对方刚失恋”“最近升职”），回车或点“添加标签”后标签会立即显示在“自定义标签”区域，可点 × 删除。自定义标签与内置标签会一起交给 AI。
   - **自定义提示词**：分两栏填写——**角色**（每行一个，可写“名字（身份/关系）”，如“小王（市场部新人）”）和**社交背景描述**（一两句话描述场景，如“公司年会，我作为新人第一次参加”）。填写后可点“**直接用提示词开始练习**”（跳过 AI 扩写），也可以先生成背景再练习。
3. **选择在场对象**：从人物库中多选参与本次场景的人物。

点击“生成社交背景”后，AI 会根据渠道、标签、角色与背景描述（以及人物库对象），按【角色】/【背景故事】两个小节结构化扩写：角色小节逐行列出每个在场角色的身份、状态与对我的态度，背景故事小节写场景气氛、谈话背景、冲突点与破冰切入点。生成的背景文字会显示在输入框中，**可直接手动修改**，确认后再开始练习。

> 在场对象来自人物库时，其**关系类型、熟悉度、所在组织/职业/所在地，以及已确认且不敏感的事实**会一并提供给 AI，用于生成背景和练习对话。

### 第二步：练习会话（“基于此背景开始练习”）

- 点击“基于此背景开始练习”会创建会话并跳转到独立的**练习会话**页面。
- 会话页左侧是历史会话列表（悬停会话可点 × **删除**，需确认，删除后不可恢复），右侧是对话区；顶部可查看渠道、标签与自动识别的场景角色，并折叠查看场景背景。
- **多人对话**：AI 一次回复可包含多个角色的连续发言，每个发言以【角色名】开头分别显示；角色之间会互相交谈、接话，用户（你）不是唯一发言者，可以随时插话或暂时旁观。
- **话题走向**：角色说话口语化、有情绪，有自己的立场，不会一直被动跟着你走，会主动提出与自己生活、职业/行业相关的话题并推进对话，让练习更接近真实社交。
- 输入框按 **Enter 发送**、**Ctrl+Enter 换行**；不知道如何回复时点“**AI 帮回**”，大模型会从你的视角给出 3 条不同策略的回复建议，点击即可直接发送，也可“换一批”或继续自己输入。
- 结束后点“结束并评分”，AI 从开场自然度、倾听能力、有效追问、共情表达、自我表达、话题衔接、边界意识、对话节奏、多角色参与、总体舒适度等维度给出 1–10 分与总结建议。

> 社交练习依赖 AI 提供商，未配置时会提示“请先配置并启用 AI 提供商”。

## 13. 复习计划

- 显示今日到期的复习任务。
- 选择“忘记 / 模糊 / 掌握 / 非常熟练”打卡，下次复习时间按间隔顺延（1 / 3 / 7 / 30 天）。

## 14. 设置

### AI 提供商与模型

见第 11 节。可新建/编辑/删除提供商，管理模型列表。

### 自定义字段定义

创建字段（单行文本 / 多行文本 / 数字 / 日期 / 链接 / 布尔等），之后可在人物详情页填写对应取值。

### 数据备份

- **导出 JSON**：下载全量业务数据（不含 API Key 明文）。
- **导入 JSON**：导入前会清空现有业务数据再写入，请谨慎使用。
- **创建备份**：SQLite 一致性快照，可下载。
- **恢复**：从备份恢复，需二次确认；恢复前会自动生成安全快照。

## 15. 长期记忆、目标与周报

- 首页“生成周报”：根据最近一周的互动生成成长报告。
- 长期记忆与表达偏好/社交目标暂以接口方式维护（`GET/PATCH /api/profile`、`GET/POST /api/memory`）；被“接受”的长期记忆会注入 AI 对话上下文。
- 人物 CSV 批量导入：调用接口 `POST /api/import/persons-csv`，列名支持 `name/姓名、nickname/昵称、organization/公司、occupation/职业、location/所在地、relationship_type/关系`。

## 16. 数据管理与迁移

### 备份与恢复

- 建议定期在设置页“创建备份”，并把备份文件复制到其他磁盘或网盘。
- 跨机器迁移：把整个 EXE 文件夹（含 `data`）复制到新机器即可；源码模式则复制项目根目录的 `data` 文件夹。

### EXE 与开发模式之间的数据迁移

两种模式使用各自独立的数据库，需要迁移时二选一：

- **直接复制数据库**（最快）：关闭两边程序后，把 `E:\Socialization\data\socialization.db` 复制到 EXE 同目录的 `data\` 下（覆盖同名文件）。若上传过文件，把 `data\uploads`、`data\backups` 也一并复制。
- **导入/导出 JSON**（可选部分数据）：在源程序设置页“导出 JSON”，再到目标程序设置页“导入 JSON”。

## 17. 数据安全提示

- 所有数据仅保存在本机，服务只监听 `127.0.0.1`。
- API Key 加密保存于数据库（密钥文件 `data/.secret.key`），前端只显示掩码，日志自动过滤。
- **删除 `data/.secret.key` 会重置全部已加密的 API Key**（需重新配置，旧密钥无法恢复）。
- 请勿把 `data` 文件夹、备份文件或导出的 JSON 随意发给他人。

## 18. 常见问题

| 问题 | 处理方式 |
|---|---|
| AI 对话报“请先配置并启用 AI 提供商” | 到设置页配置提供商、测试连接、选择模型，并在对话页选择提供商与模型 |
| 简报 / 提取 / 复盘 / 练习 / 背景生成报 AI 错误 | 确认提供商“测试连接”通过且已启用可用模型 |
| EXE 首次启动很慢 | 单文件程序需要解包并建库，等待 10–30 秒属正常 |
| 杀毒软件拦截 EXE | 确认来源可信后选择“允许”，或将 EXE 加入白名单 |
| 提示缺少 WebView2 | 到微软官网安装“WebView2 Runtime”后重试 |
| EXE 打开后没有我原来的数据 | EXE 与开发模式数据不互通，按第 16 节迁移；或检查数据文件夹是否与 EXE 在同一目录 |
| EXE 端口 8765 被占用 | 关闭占用 8765 端口的程序后重启 EXE |
| 开发模式端口被占用 | 关闭占用 3000/8000 的程序后重新运行 `dev.ps1` |
| 上传文件状态“解析失败” | 文件页点“处理”重试；图片 OCR / 音视频转写需要提供商支持对应能力 |
| 笔记自动保存提示冲突 | 内容可能被其他编辑修改，刷新后重试 |
| 忘记 API Key | 到设置页清除后重新输入（密钥文件未删除则无需重配全部） |
